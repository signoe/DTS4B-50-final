import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        type: 'dark',
        primary: {
          main: '#0d47a1',
        },
        secondary: {
          main: '#c51162',
        },
      },
});

export default theme;
